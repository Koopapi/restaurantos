# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# pubspec.lock se versiona (app): builds reproducibles en un clon limpio.

# IDE
.idea/
*.iml
.vscode/

# Android / iOS generados (se regeneran con `flutter create .`)
android/.gradle/
android/local.properties
android/key.properties
ios/Pods/
ios/.symlinks/
*.log
